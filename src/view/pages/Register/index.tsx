import AuthForm from "../../components/Auth/components/AuthForm.tsx";

export default function Register() {
  return (
    <AuthForm type="register" title="Crie sua conta" subtitle="Já possui uma conta?" />
  );
}
